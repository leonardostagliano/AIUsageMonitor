<Window x:Class="AIUsageMonitor.App.Updates.UpdatePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AIUsageMonitor · Aggiornamento" Width="460" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" Background="{StaticResource WindowBackground}"
        FontFamily="{StaticResource TextFont}" FontSize="13" Foreground="{StaticResource TextPrimary}">
  <!-- Stessa pelle delle Impostazioni: stili impliciti di Assets/Controls.xaml, barra del titolo scurita da DWM in
       OnSourceInitialized. Gli errori usano WarningText come i messaggi di stato del notch. -->
  <StackPanel Margin="24,20,24,20">
    <TextBlock Text="Aggiornamento disponibile" FontFamily="{StaticResource DisplayFont}" FontSize="17" FontWeight="SemiBold" Margin="0,0,0,10"/>
    <TextBlock Text="{Binding Lead, Mode=OneWay}" TextWrapping="Wrap"/>
    <TextBlock Text="{Binding Note, Mode=OneWay}" Foreground="{StaticResource TextMuted}" FontSize="12" TextWrapping="Wrap" Margin="0,6,0,0"/>

    <Border Visibility="{Binding ProgressVisibility, Mode=OneWay}" Background="{StaticResource Card}" CornerRadius="14" Padding="14,12" Margin="0,16,0,0">
      <StackPanel>
        <DockPanel>
          <TextBlock DockPanel.Dock="Right" Text="{Binding PercentText, Mode=OneWay}" Foreground="{StaticResource TextMuted}" Margin="8,0,0,0"/>
          <TextBlock Text="{Binding ProgressLabel, Mode=OneWay}" TextTrimming="CharacterEllipsis"/>
        </DockPanel>
        <ProgressBar Margin="0,8,0,0" Minimum="0" Maximum="100" Value="{Binding ProgressValue, Mode=OneWay}"
                     IsIndeterminate="{Binding IsIndeterminate, Mode=OneWay}"/>
      </StackPanel>
    </Border>

    <TextBlock Text="{Binding Error, Mode=OneWay}" Visibility="{Binding ErrorVisibility, Mode=OneWay}" Foreground="{StaticResource WarningText}"
               TextWrapping="Wrap" Margin="0,12,0,0"/>

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
      <Button Content="Più tardi" Command="{Binding LaterCommand}" IsCancel="True"/>
      <Button Content="{Binding ConfirmLabel, Mode=OneWay}" Command="{Binding ConfirmCommand}" Style="{StaticResource PrimaryButton}" Margin="0"/>
    </StackPanel>
  </StackPanel>
</Window>
